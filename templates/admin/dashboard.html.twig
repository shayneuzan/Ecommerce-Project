<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>{% block title %}Admin Dashboard - Traventa{% endblock %}</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="{{ basePath }}/assets/css/style.css">
    <link rel="stylesheet" href="{{ basePath }}/assets/css/adminStyle.css">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-navy sticky-top py-4">
        <div class="container d-flex justify-content-between align-items-center">
            <div>
                <h1 class="page-header-title">Admin Dashboard</h1>
                <h6 class="page-header-subtitle">Manage packages, bookings, and view analytics</h6>
            </div>
            <div class="d-flex align-items-center gap-2">
                <form action="{{ basePath }}/auth/logout" method="POST" class="m-0">
                    <button type="submit" class="btn btn-danger">Logout</button>
                </form>
            </div>
        </div>
    </nav>

    {# ── Flash messages ── #}
    {# Moved to admin/index.html.twig for better visiblity, don't know what to do with this. Remove or not? #}
    {# {% if flash is defined %}
        <div class="flash-container">
            {% for type, messages in flash %}
                {% for message in messages %}
                    <div class="alert alert-{{ type }} alert-dismissible fade show mb-0 text-center" role="alert">
                        {% if type == 'success' %}
                            ✔
                        {% elseif type == 'danger' %}
                            ✖
                        {% elseif type == 'warning' %}
                            ⚠
                        {% elseif type == 'info' %}
                            i
                        {% endif %}
                        
                        {{ message }}

                        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                    </div>
                {% endfor %}
            {% endfor %}
        </div>
    {% endif %} #}
    

    {% block content %}{% endblock %}

    <footer class="footer py-4">
        <div class="container text-center">
            <p class="mb-0 text-white">&copy; 2026-Present Traventa</p>
        </div>
    </footer>
</body>

{% block scripts %}{% endblock %}
</html>